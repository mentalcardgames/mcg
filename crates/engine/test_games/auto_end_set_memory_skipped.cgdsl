// Repro (updated 2026-08-10): after the last player is eliminated, the
// remaining instructions of the out-of-game current player are skipped -
// Counter is 5 never runs and the stage auto-ends. (SetMemory still
// errors without a current player when reached at top level.)
player P1
turnorder (P:P1)

stage Play for current 1 times {
  set P:P1 out of game
  end turn
  Counter is 5
}
