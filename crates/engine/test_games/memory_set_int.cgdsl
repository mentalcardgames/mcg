// Exercise: M is 42 sets memory M to Int(42). Declared on table, the
// write targets the declared slot (Table_M) - D-14 fixed 2026-08-10.
player P1
turnorder (P:P1)
memory M on table

stage Play for current 1 times {
  M is 42
  end stage
}
