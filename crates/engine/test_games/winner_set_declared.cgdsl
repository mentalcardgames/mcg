// Winner set at game end (2026-08-10): `end game with winner P:P1` declares
// the winner — everyone not named is eliminated, so the survivors are the
// winner set.
player P1, P2
turnorder (P:P1, P:P2)

stage Play for current 1 times {
  end game with winner P:P1
}
