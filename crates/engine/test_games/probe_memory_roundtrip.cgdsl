// Probe: memory set + read with explicit owner round-trip
// M is 42 uses current-player prefix, (&I:M of P:P1) reads with explicit owner
player P1, P2
turnorder (P:P1, P:P2)
memory M on table

stage Play for current 1 times {
    M is 42
    score (&I:M of P:P1) to P:P1
    end stage
}
